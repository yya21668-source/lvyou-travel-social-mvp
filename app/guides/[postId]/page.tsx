import { Avatar, VerifiedBadge } from "@/components/ui";
import { guides } from "@/mocks/data";
import { ArrowLeft, Bookmark, Heart, MapPin, Share2, Sparkles } from "lucide-react";
import Image from "next/image";
import Link from "next/link";

export default function GuidePage({ params }: { params: { postId: string } }) { const guide = guides.find(g => g.id === params.postId) || guides[0]; return <article className="min-h-dvh pb-12"><div className="relative h-[55vh]"><Image src={guide.image} alt={guide.title} fill priority className="object-cover"/><div className="absolute inset-0 bg-gradient-to-t from-ink via-transparent to-ink/20"/><div className="absolute left-5 right-5 top-[max(1.25rem,env(safe-area-inset-top))] flex justify-between"><Link href="/discover" className="grid size-10 place-items-center rounded-full bg-white/80 backdrop-blur"><ArrowLeft size={19}/></Link><button className="grid size-10 place-items-center rounded-full bg-white/80 backdrop-blur"><Share2 size={18}/></button></div><div className="absolute bottom-7 left-5 right-5 text-white"><p className="text-xs font-bold tracking-[.16em]">{guide.destination} · {guide.style}</p><h1 className="mt-3 text-[34px] font-black leading-tight">{guide.title}</h1><div className="mt-5 flex items-center gap-2"><Avatar label={guide.avatar}/><b className="text-sm">{guide.author}</b><VerifiedBadge compact/></div></div></div><div className="px-5 pt-6"><div className="rounded-card bg-lake-300/15 p-5"><div className="flex items-center gap-2 text-sm font-bold text-lake-500"><Sparkles size={17}/>AI 摘要</div><ul className="mt-3 space-y-2 text-sm text-ink/65">{guide.summary.map(s => <li key={s}>• {s}</li>)}</ul></div><div className="mt-7 text-[15px] leading-8 text-ink/70"><p>到大理的第一天，我没有安排很多景点。风从洱海吹过来，路边的树影和水光已经足够构成一整天。</p><h2 className="mt-7 text-2xl font-black text-ink">沿着洱海，把速度慢下来</h2><p className="mt-3">从才村租一辆自行车，沿生态廊道向北。上午的风比较轻，途中可以随时停下。喜洲不要只逛主街，转进巷子里会遇到老宅、扎染小院和刚出炉的粑粑。</p><div className="my-6 flex items-center gap-3 rounded-2xl bg-white p-4"><MapPin className="text-coral"/><div><b className="text-sm text-ink">路线提示</b><p className="text-xs text-ink/45">才村 → 廊桥 → 喜洲，骑行约 24km</p></div></div><p>这趟旅行最喜欢的，不是完成了多少打卡，而是每个人都可以坦然说出“我想停一下”。好的同行，是节奏可以彼此看见。</p></div><div className="mt-8 flex gap-3"><button className="pill flex flex-1 items-center justify-center gap-2 bg-white"><Heart size={18}/> {guide.likes}</button><button className="pill flex flex-1 items-center justify-center gap-2 bg-white"><Bookmark size={18}/>收藏</button></div><p className="mt-6 text-center text-[11px] text-ink/35">公开攻略 · 未登录用户可只读访问</p></div></article>; }
