import { Avatar, VerifiedBadge } from "@/components/ui";
import { ArrowLeft, ArrowRight, CalendarDays, MapPin, Route, ShieldCheck } from "lucide-react";
import Link from "next/link";

const travelers = [{ id: "app-1", name: "阿禾", avatar: "禾", match: 92, bio: "建筑摄影师，喜欢早起、骑行和在菜市场吃早餐。", tags: ["9.18 出发", "度假", "洱海骑行"] }, { id: "app-2", name: "林屿", avatar: "屿", match: 86, bio: "第一次去大理，慢热但靠谱，想认真看一次日出。", tags: ["时间重合 3 天", "轻徒步", "摄影"] }];
export default function MatchesPage() { return <div className="px-5 pb-28 pt-[max(1.25rem,env(safe-area-inset-top))]"><header className="flex items-center justify-between"><Link href="/trips/yunnan-summer/mode" className="grid size-10 place-items-center rounded-full bg-white"><ArrowLeft size={19}/></Link><span className="text-sm font-bold">相似行程</span><span className="w-10"/></header><section className="mt-9"><p className="eyebrow">TRIP MATCH</p><h1 className="mt-2 text-3xl font-black">有人正吹着同一阵风</h1><p className="mt-2 text-sm text-ink/50">根据目的地、日期和旅行偏好为你推荐。</p></section><div className="mt-6 space-y-4">{travelers.map((t, i) => <article key={t.id} className="glass rounded-card p-5"><div className="flex items-start gap-3"><Avatar label={t.avatar} className="size-12"/><div className="flex-1"><div className="flex items-center gap-2"><h2 className="font-bold">{t.name}</h2>{i === 0 && <VerifiedBadge compact/>}</div><p className="mt-1 text-xs text-ink/45">信用良好 · 已完成 {i + 2} 次同行</p></div><span className="rounded-full bg-coral/15 px-3 py-1 text-xs font-black text-coral">{t.match}%</span></div><p className="mt-4 text-sm leading-6 text-ink/60">{t.bio}</p><div className="mt-3 flex flex-wrap gap-2">{t.tags.map(tag => <span key={tag} className="rounded-full bg-sand px-3 py-1.5 text-[11px]">{tag}</span>)}</div><Link href={`/matches/${t.id}/confirm`} className="mt-5 flex items-center justify-between border-t border-ink/5 pt-4 text-sm font-bold text-lake-500">查看同行资料并申请 <ArrowRight size={18}/></Link></article>)}</div><div className="mt-5 flex gap-3 rounded-2xl bg-forest-500/10 p-4 text-xs leading-5 text-forest-700"><ShieldCheck size={18} className="shrink-0"/><p>不要在确认前分享身份证、住址等敏感信息。首次同行建议选择公共场所见面。</p></div></div>; }
