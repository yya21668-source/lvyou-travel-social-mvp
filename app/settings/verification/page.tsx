"use client";

import { Button } from "@/components/ui";
import { ArrowLeft, BadgeCheck, Check, LockKeyhole, PhoneCall } from "lucide-react";
import Link from "next/link";
import { useState } from "react";

export default function VerificationPage(){const [done,setDone]=useState(false);return <div className="min-h-dvh px-5 pb-10 pt-[max(1.25rem,env(safe-area-inset-top))]"><header className="flex items-center justify-between"><Link href="/profile" className="grid size-10 place-items-center rounded-full bg-white"><ArrowLeft size={19}/></Link><span className="text-sm font-bold">安全与信任</span><span className="w-10"/></header><section className="mt-10"><span className="grid size-14 place-items-center rounded-2xl bg-lake-300/20 text-lake-500"><BadgeCheck size={28}/></span><h1 className="mt-5 text-3xl font-black">真实一点，同行更安心</h1><p className="mt-3 text-sm leading-6 text-ink/50">认证信息仅用于安全校验，不会展示证件号码等敏感数据。</p></section><div className="glass mt-7 rounded-card p-5"><div className="flex items-center justify-between"><div><b>实名认证</b><p className="mt-1 text-xs text-ink/40">MVP 状态模拟</p></div><span className={`rounded-full px-3 py-1 text-xs font-bold ${done?"bg-forest-500/15 text-forest-500":"bg-coral/15 text-coral"}`}>{done?"已认证":"未认证"}</span></div><Button onClick={()=>setDone(true)} disabled={done} className="mt-5 w-full">{done?<><Check/>认证完成</>:"模拟认证通过"}</Button><p className="mt-3 text-[10px] leading-4 text-ink/35">MVP 阶段 mock，V2 需接入真实身份认证服务。</p></div><div className="mt-4 rounded-card bg-white p-5"><div className="flex items-center gap-3"><PhoneCall className="text-coral"/><div><b className="text-sm">紧急联系人</b><p className="text-xs text-ink/40">妈妈 · 138 **** 0821</p></div></div></div><div className="mt-4 flex gap-3 rounded-2xl bg-forest-500/10 p-4 text-xs leading-5 text-forest-700"><LockKeyhole size={18} className="shrink-0"/>我们遵循最小必要原则保存安全信息。真实服务接入后将使用加密和访问审计。</div></div>}
